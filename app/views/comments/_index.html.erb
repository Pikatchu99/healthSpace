<ul>
  <% comments.each do |comment| %>
    <% if comment.id.present? %>
      <li>
        <div class="comment-content" id ="this_comment_<%= comment.id %>"></div>
        <p><%= comment.content %></p>
        <p><%= link_to "edit", edit_pharmacy_comment_path(pharmacy, comment), remote: true %></p>
        <p><%= link_to "delete", pharmacy_comment_path(comment.pharmacy_id, comment.id), method: :delete, remote:true,data: { confirm: "êtes vous sure ?"} %></p>
      </li>
    <% end %>
  <% end %>
</ul>
